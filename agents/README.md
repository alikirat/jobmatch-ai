# agents

Google ADK 2.0 graph workflow for JobMatch AI.

```
agents/
├── workflows/   graph definitions wiring nodes together
└── nodes/       individual node implementations (ingest, score, gap analysis, optimize)
```
